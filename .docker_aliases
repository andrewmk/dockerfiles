alias get-iplayer="docker run --rm -it -v `pwd`:/tmp/get-iplayer alexhaydock/get-iplayer"
alias get_iplayer="docker run --rm -it -v `pwd`:/tmp/get-iplayer alexhaydock/get-iplayer"
alias glances="docker run --rm -it --pid host alexhaydock/glances"
alias htop="docker run --rm -it --pid host alexhaydock/htop"
alias massarchive="docker run --rm -it alexhaydock/massarchive"
alias onionscan="docker run --rm -it alexhaydock/onionscan"
alias smartctl="docker run --rm --privileged alexhaydock/smartctl"
alias youtube-dl="docker run --rm -it -v `pwd`:/tmp/youtube-dl alexhaydock/youtube-dl"
alias youtube_dl="docker run --rm -it -v `pwd`:/tmp/youtube-dl alexhaydock/youtube-dl"
